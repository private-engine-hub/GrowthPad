/// <reference types="react" />
interface PillarProps {
    header?: React.ReactNode;
    children: React.ReactNode;
}
export declare function PillarColumn({ header, children }: PillarProps): JSX.Element;
export {};
